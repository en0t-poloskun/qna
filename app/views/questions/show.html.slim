h1= @question.title
b= "#{@question.author.email} asks:"
p= @question.body

= render 'shared/files', resource: @question

.answers
  = render @answers

h3= 'Add answer'

= render 'answers/form'

= link_to 'Back', questions_path
